package Algorithm_and_Arrays;

public class Algorithm_Complexity {

	/*
	Bad Linear Search
		worst-case - O(N), because you will need to iterate through
					 the entire array for searching necessary element 
					 
	arrayIndexItemAccess 
		worst-case - O(1), because you have index of the element so it
					 takes constant time 
	
	binarySorting 
		worst-case - O(N log N), because the array is sorted and you need 
					 only iterate through the entire array
	
	twoCycleSorting - O(N), because we will need to iterate through the 
					  entire array twice
    */
}
